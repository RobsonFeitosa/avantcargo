services:
  api:
    volumes:
      - ./api:/app
      - /app/node_modules
    command: sh -c "yarn typeorm:cli migration:run && yarn dev"

  web:
    volumes:
      - ./web:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev